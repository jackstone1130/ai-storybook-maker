---
name: ai-storybook-maker
description: 把故事文本做成「会动、会念、能指读」的单文件交互式绘本 HTML —— AI 插画 + 逐字高亮朗读 + 跟说句 + 可打印。适用于给孩子做启蒙读物、语言训练材料、识字故事书。触发词：做绘本、故事书、图画书、AI 插画绘本、指读绘本、点读绘本、storybook、picture book。
---

# AI 交互式绘本制作

把一段故事文本，做成**浏览器双击就能用**的绘本。成品是一个**单文件 HTML**——插画、音频全部 base64 内嵌，拷到哪都能用，离线可播。

## 成品形态

```
一部绘本 = 若干故事 × 每个故事 N 格
每一格 = 插画 + 大字长句 + 绿框跟说句 + 家长提示
```

| 能力 | 说明 |
|---|---|
| 逐字指读 | 念到哪个字，哪个字亮（橙底长句 / 绿底跟说句） |
| 长句 + 跟说句 | 先念完整长句，再念一句短的让孩子跟说 |
| 持续听 | 依次播各页长句，边播边翻页边高亮 |
| 推拉镜头 | 插画缓慢放大平移（Ken Burns），避免静态呆板 |
| 单文件 | 无外部依赖，离线可用，可直接分享 |
| 可打印 | 「展开全部画面」→ 浏览器打印成一册 |

---

## 六步流程

### Step 1 · 设计故事文本

**每格两层，这是这个技能的核心设计：**

```
家长读：小兔子先走到洗手台，把两只小手洗得干干净净。   ← 练「听长句」
↳ 孩子说：小兔子先洗手。                              ← 练「开口」
```

- 长句 15-22 字，带连接词、修饰语、对话、拟声词
- 短句 5-10 字，是孩子实际要说的
- **不要求孩子复述长句**——长句练听，短句练说，分开
- 每格配一条 `hint`（家长提问/动作引导）
- 句型词用 `<em>` 标记（先…然后…最后、因为…所以…、里面/下面）

### Step 2 · 生成插画

用图像生成工具，**逐格生成**。角色一致性靠**逐字相同的角色描述串**：

```
[固定角色串] + [固定风格串] + Scene: [本格场景]
+ IMPORTANT: the image must contain absolutely no text, no letters, no words,
  no numbers, no captions, no logo and no watermark anywhere.
```

- **角色串每个故事内必须逐字一致**，只改 `Scene:` 之后
- 跨故事复用角色时，把角色串原样搬过去
- 那句禁文字指令**必须加**，否则模型会往画面里塞 `MILK`、`YUM!` 这类英文标注
- 比例建议 3:2（如 1536×1024），与绘本格接近

### Step 3 · 图片后处理

用 `scripts/prep_images.py`：**备份原图 → 去水印 → 压缩**。

- 平台水印（这类工具常强制加）**必须后期抹**，参数和 prompt 都关不掉
- 去水印正解：取水印区**左侧等宽区域横向镜像后原样粘贴**（接缝天然连续）
- **绝不要做整块高斯模糊**，会留明显色块
- **操作前先备份原图**——否则返工时原图已毁

### Step 4 · 生成朗读音频

用 `scripts/make_tts.py`（macOS `say`）：

- **长句、短句拆成两段独立音频**（`sXpY` / `sXpYs`），页面顺序播放 → 天然知道切换点，同步才准
- **不要合成成一整段**，那样无法精确对齐高亮
- 高级音色优先：`say -v '?'` 找带 `(Premium)` / `(Enhanced)` 的
- 语速 125-135
- 标点后插停顿：`[[slnc 320]]`；句末 `[[slnc 230]]`
- **不要用 `[[pbas]]` 做音高起伏**（见下）

### Step 5 · 组装 HTML

数据形如：

```js
const S = [{
  title: "故事名", tags: ["句型：…", "目标音：…"],
  pages: [
    { hint: "家长提示", art: `<img src="data:image/jpeg;base64,…">`,
      txt: `长句，<em>句型词</em>高亮`, say: `孩子跟说的短句` },
  ]
}];
```

页面关键机制：

- `wrapChars(html)` 把文字拆成单字 `<i class="c">`，供逐字高亮
- `tickHL(a, el)` 用 `requestAnimationFrame` 读 `currentTime/duration` 按字数比例点亮
- `playAudio(key, fallback, done, hlEl)` 三级回落：内嵌 base64 → 外部文件 → 系统语音
- 播放失败**必须显式提示**（`noteEnv()`），不能静默降级

### Step 6 · 验证

**必须用无头浏览器验证**，不要靠肉眼：

```bash
"$CHROME" --headless=new --disable-gpu --no-sandbox \
  --enable-logging=stderr --virtual-time-budget=8000 \
  --dump-dom "file:///绝对路径.html" 2>err.log >dom.html

grep -iE "uncaught|TypeError|ReferenceError" err.log   # 有无运行时错误
grep -o 'class="page' dom.html | wc -l                 # 应等于 故事数×格数
```

`scripts/check_page.py` 已封装此流程。

---

## 硬性约定（每条都是踩坑换来的）

1. **交付物一律单文件自包含**（图片、音频 base64 内嵌）。预览类服务通常只映射单个 HTML，同目录资源一律 404。
2. **面向「浏览器直接打开」设计**，不假定预览面板能播媒体——预览面板往往**不出声**。
3. **禁止静默降级**：播放失败必须显式提示，否则会被误判成内容问题。
4. **逐字高亮是刚需**，不是装饰：受众要靠「听到的音 + 看到的字」建立对应。
5. **长句 / 跟说句必须是两段独立音频**，否则同步点不准。
6. **`[[pbas]]` 慎用**：它是**整体升降调**，不是自然的语调曲线。幅度大或多处切换会让人声发飘。宁可不用。
7. **`transform: scale()` 动效的父容器必须 `overflow:hidden`**，否则放大的图会压到相邻文字。
8. **替换大段代码时不要用远处的大锚点**（如 `index('const A')` → `index('const B')`），中间可能夹着别的代码，会静默删掉。用**紧邻目标的锚点**。
9. **`Path.mkdir(exist_ok=True)` 在受限沙箱下对已存在目录仍会抛错**，临时目录改用 `tempfile.mkdtemp()`。
10. **验证 CSS 动画用 `getAnimations()`**，不要用截图比对——无头环境不渲染动画中间帧，会得出错误结论。
11. 长跑脚本**不要用 `head -N` 接管道**，会因 SIGPIPE 提前中断。
12. **按钮默认隐藏**，长按画面 1.2 秒唤出 —— 防止孩子乱点设置。长按判定要加位移容差（>8px 取消），否则孩子滑动会误触。

---

## 脚本

| 脚本 | 用途 |
|---|---|
| `scripts/make_tts.py` | 读 HTML 里的故事数据 → 批量合成朗读音频 → base64 内嵌回 HTML |
| `scripts/prep_images.py` | 备份原图 → 去平台水印 → 压缩成网页尺寸 |
| `scripts/check_page.py` | 无头浏览器诊断：渲染数量 + 控制台错误 |

三个脚本都在文件头部有可调常量（音色、语速、水印坐标、目标尺寸等）。

---

## 参考

- `references/pitfalls.md` — 完整踩坑清单与排查手法
- `references/html-notes.md` — 页面结构与交互机制说明
